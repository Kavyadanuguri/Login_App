// Write your code here
import './index.css'

const Logout = props => {
  const {value2} = props

  return (
    <button type="button1" onClick={value2} className="button">
      Logout
    </button>
  )
}

export default Logout
